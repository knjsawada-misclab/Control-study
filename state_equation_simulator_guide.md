# 2次元線形システムの相平面シミュレータ

大阪大学 応用理工学科 機械工学科目3年「制御工学」(澤田賢治) の講義で扱う，自律系 $\dot{x} = Ax$ ( $x \in \mathbb{R}^2$ ) の位相面(相平面)と時間応答をブラウザ上で可視化するシミュレータです．

> **シミュレータ**：`state_equation_simulator.html` をブラウザで開くだけで動作します（外部ライブラリ不要・オフライン可・スマホ/タブレット対応）．行列 $A$ の要素をスライダーで変え，固有値・平衡点の分類・位相面・時間応答がリアルタイムに更新されます．

---

## デモ / 使い方

```bash
git clone https://github.com/knjsawada-misclab/Control-study.git
cd Control-study
# state_equation_simulator.html をブラウザで開く
open state_equation_simulator.html        # macOS
# xdg-open state_equation_simulator.html  # Linux
# start state_equation_simulator.html     # Windows
```

操作のポイント：

- **行列 A** のスライダー ( $a_{11}, a_{12}, a_{21}, a_{22}$ ，範囲 $-3$ 〜 $3$ ) を動かすとシステムが即座に更新されます．数値入力欄で細かい値の指定も可能です．
- **システム特性** (トレース tr(A)，行列式 det(A)，判別式 $\Delta$ ) と**固有値** $\lambda_1, \lambda_2$ がサイドバーに表示されます．
- **平衡点の分類**がバッジで表示されます (安定節点・不安定節点・鞍点・安定焦点・不安定焦点・中心・退化)．
- **初期値** $x_0 = (x_{10}, x_{20})$ を数値入力し「適用」ボタンまたは Enter で軌道を再計算します．
- **位相面** (左パネル)：ベクトル場・固有ベクトル方向・軌道を表示．ドラッグで移動，ホイールでズーム，ダブルクリックでリセット．
- **時間応答** (右パネル)： $x_1(t)$ , $x_2(t)$ の波形を表示．ドラッグで移動，ホイールで時間軸ズーム，Shift+ホイールで縦軸ズーム．
- **プリセット**ボタンで代表的な7パターン (安定節点・不安定節点・鞍点・安定焦点・不安定焦点・中心・安定節点(重根)) を切り替え，典型的な振る舞いを確認できます．
- **固有値の意味**欄に，実部・虚部が解の収束/発散・振動にどう対応するかの解説が表示されます．

---

## 状態方程式と解

2次元の自律線形システムは次の状態方程式で記述されます．

```math
\dot{x}(t) = Ax(t), \qquad A = \begin{bmatrix} a_{11} & a_{12} \\ a_{21} & a_{22} \end{bmatrix}, \quad x(t) = \begin{bmatrix} x_1(t) \\ x_2(t) \end{bmatrix}
```

初期値 $x(0) = x_0$ に対する解は**行列指数関数**で与えられます．

```math
x(t) = e^{At}\, x_0
```

行列指数関数 $e^{At}$ は次のいずれかの方法で計算できます．

- **ラプラス変換**（レゾルベント行列の逆ラプラス変換）

```math
e^{At} = \mathcal{L}^{-1}\!\left[(sI - A)^{-1}\right]
```

- **対角化**： $A = P \Lambda P^{-1}$ のとき

```math
e^{At} = P\, e^{\Lambda t}\, P^{-1}
```

- **ジョルダン形**： $A$ が対角化不可能な場合は $A = P J P^{-1}$ として

```math
e^{At} = P\, e^{Jt}\, P^{-1}
```

---

## 固有値と解の振る舞い

$A$ の固有値 $\lambda_1, \lambda_2$ は特性方程式を解いて求めます．

```math
\det(\lambda I - A) = \lambda^2 - \mathrm{tr}(A)\lambda + \det(A) = 0
```

```math
\lambda_{1,2} = \frac{\mathrm{tr}(A) \pm \sqrt{\Delta}}{2}, \quad \Delta = \mathrm{tr}(A)^2 - 4\det(A)
```

解の振る舞いは固有値の実部と虚部で決まります．

| 固有値の性質 | 解の振る舞い |
|---|---|
| 実部 Re(λ) < 0 | 時間とともに**収束**（減衰） |
| 実部 Re(λ) > 0 | 時間とともに**発散**（増大） |
| 実部 Re(λ) = 0 | 振幅一定（**持続振動**または静止） |
| 虚部 Im(λ) ≠ 0 | **振動**成分をもつ（周期 $T = 2\pi / \omega$） |
| 虚部 Im(λ) = 0 | 振動なし（**単調**な収束/発散） |

固有値が複素数 $\lambda = \sigma \pm j\omega$ のとき，解は $e^{\sigma t}$ の包絡線をもちながら角周波数 $\omega$ で振動します． $\sigma < 0$ なら減衰振動， $\sigma > 0$ なら発散振動， $\sigma = 0$ なら等振幅の周期運動 (中心) です．

---

## 相平面の分類

2次元線形システムの平衡点（原点）は，固有値の組合せにより以下の7パターンに分類されます．

| # | 名称 | 固有値の条件 | 判別式・tr・det の条件 | 位相面の様子 |
|---|---|---|---|---|
| 1 | **安定節点** (Stable Node) | $\lambda_2 < \lambda_1 < 0$（実数，両方負） | $\Delta \ge 0$，tr $< 0$，det $> 0$ | 全軌道が原点に収束．遅い固有ベクトル方向に接線的に接近 |
| 2 | **安定焦点** (Stable Spiral) | $\sigma \pm j\omega$，$\sigma < 0$（複素，負実部） | $\Delta < 0$，tr $< 0$ | 原点に渦巻き状に収束（減衰振動） |
| 3 | **不安定節点** (Unstable Node) | $0 < \lambda_1 < \lambda_2$（実数，両方正） | $\Delta \ge 0$，tr $> 0$，det $> 0$ | 全軌道が原点から発散．速い固有ベクトル方向に沿って離脱 |
| 4 | **不安定焦点** (Unstable Spiral) | $\sigma \pm j\omega$，$\sigma > 0$（複素，正実部） | $\Delta < 0$，tr $> 0$ | 原点から渦巻き状に発散（発散振動） |
| 5 | **鞍点** (Saddle) | $\lambda_1 < 0 < \lambda_2$（実数，異符号） | det $< 0$ | 安定固有ベクトル方向に収束，不安定固有ベクトル方向に発散 |
| 6 | **中心** (Center) | $\pm j\omega$（純虚数） | $\Delta < 0$，tr $= 0$ | 原点の周りを周回する閉軌道（持続振動） |
| 7 | **退化** (Degenerate) | 重根 $\lambda_1 = \lambda_2$ または det $= 0$ | $\Delta = 0$ | 安定星型節点，退化節点，直線上の流れなど |

> **シミュレータの対応**：プリセットボタンで上記の各パターンを瞬時に設定できます．バッジの色分け（緑=安定，赤=不安定，橙=鞍点，紫=中心，灰=退化）で分類が一目で分かります．

---

## 不変部分空間

### 実固有値の場合

$A$ が実固有値 $\lambda_i$ と固有ベクトル $v_i$ をもつとき，$v_i$ の張る1次元部分空間は $A$ に対して**不変**です．

```math
Av_i = \lambda_i v_i
```

軌道が一度固有ベクトル方向に入ると，その直線上にとどまり続けます．解は次のように書けます．

```math
x(t) = c_1 e^{\lambda_1 t} v_1 + c_2 e^{\lambda_2 t} v_2
```

ここで $c_1, c_2$ は初期値 $x_0$ から決まる係数です．

**安定節点** ( $\lambda_2 < \lambda_1 < 0$ ) の場合： $|\lambda_2| > |\lambda_1|$ より $e^{\lambda_2 t}$ の方が速く減衰するため，十分時間が経つと $e^{\lambda_1 t} v_1$ 成分が支配的になります．つまり，軌道は最終的に**遅い固有ベクトル** $v_1$ の方向に近づきながら原点に収束します．

### 複素固有値の場合

$A$ が複素共役固有値 $\lambda = \sigma \pm j\omega$ をもち，固有ベクトルが $v = v_r + jv_i$ のとき，Re(v) と Im(v) の張る2次元部分空間が不変です．

```math
\mathrm{span}\{v_r, v_i\} \text{ は } A \text{ に対して不変}
```

2次元系 ( $n = 2$ ) ではこれが全空間 $\mathbb{R}^2$ そのものに一致するため，全ての軌道がこの部分空間内で渦巻き運動を行います．

---

## シミュレータで確認できること

以下のポイントをシミュレータで実験的に確認してみてください．

1. **tr-det 図との対応**：tr(A) と det(A) の符号・大きさを変えて，分類が切り替わる境界を探ってみましょう．
2. **固有ベクトル方向の確認**：位相面に表示される固有ベクトル方向の直線が不変部分空間に対応します．鞍点では安定/不安定方向が明確に見えます．
3. **速いモードと遅いモードの分離**：安定節点で $\lambda_1$ と $\lambda_2$ の差を大きくすると，速い成分が先に減衰し，軌道が遅い固有ベクトル方向に「折れ曲がる」様子が観察できます．
4. **振動の周期**：安定焦点や中心で，時間応答の波形から振動周期 $T = 2\pi / \omega$ を読み取り，固有値の虚部と一致することを確認できます．
5. **収束・発散速度**：固有値の実部 $\sigma$ を変えると，時間応答の減衰/増大の速さが変わります．実部の絶対値が大きいほど速い応答になります．
6. **鞍点の安定多様体**：鞍点で初期値を安定固有ベクトル方向に正確に置くと原点に収束しますが，わずかにずれると不安定方向に発散します．

---

## パラメータ一覧

| パラメータ | 説明 | 範囲 / 既定値 |
|---|---|---|
| $a_{11}, a_{12}, a_{21}, a_{22}$ | 行列 $A$ の要素 | $-3$ 〜 $3$ (ステップ $0.05$ ) |
| $x_{10}$ | 初期値 $x_0$ の第1成分 | 既定 $1.5$ |
| $x_{20}$ | 初期値 $x_0$ の第2成分 | 既定 $1.0$ |

### プリセット一覧

| プリセット名 | 分類 |
|---|---|
| 安定節点 (Stable Node) | 実固有値，両方負，異なる値 |
| 不安定節点 (Unstable Node) | 実固有値，両方正，異なる値 |
| 鞍点 (Saddle Point) | 実固有値，異符号 |
| 安定焦点 (Stable Spiral) | 複素固有値，負の実部 |
| 不安定焦点 (Unstable Spiral) | 複素固有値，正の実部 |
| 中心 (Center) | 純虚数固有値 |
| 安定節点 (重根) | 実重根，負 |

---

*この資料は付属の HTML シミュレータ（`state_equation_simulator.html`）の理論背景と操作方法を記述したものです．*
