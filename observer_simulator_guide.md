# 同一次元オブザーバ シミュレータ

制御工学（応用理工学科機械工学科目3年）／大阪大学 澤田賢治

入出力データから内部状態を推定する**同一次元オブザーバ**(状態観測器)のインタラクティブシミュレータです．システム行列 $(A, B, C)$ とオブザーバゲイン $L$ を設定すると，実システムとオブザーバの時間応答を同時にシミュレーションし，推定誤差の収束過程を可視化します．

> **対象**：線形時不変の状態空間モデル $\dot{x} = Ax + Bu$ , $y = Cx$ (2次系) に対して，出力 $y(t)$ と入力 $u(t)$ から内部状態 $x(t)$ を推定するオブザーバの設計法を体験的に学ぶシミュレータです．

---

## デモ / 使い方

付属の `observer_simulator.html` を **ブラウザで開くだけ**で動作します（外部ライブラリ不要・オフライン可）．

```bash
git clone https://github.com/knjsawada-misclab/Control-study.git
cd Control-study
# observer_simulator.html をブラウザで開く（ダブルクリック or 下記）
open observer_simulator.html        # macOS
# xdg-open observer_simulator.html  # Linux
# start observer_simulator.html     # Windows
```

操作のポイント：

- **システム行列** $A$ (2x2), $B$ (2x1), $C$ (1x2) の各要素を入力欄で編集．
- **初期状態** $x_0 = (x_1(0), x_2(0))$ を実システムの初期値として設定．
- **オブザーバ初期状態** $\hat{x}_0 = (\hat{x}_1(0), \hat{x}_2(0))$ を設定（実状態と異なる値にすることで推定誤差の収束を観察）．
- **オブザーバゲイン** $L$ (2x1) を直接指定，または目標極を指定して自動計算．
- **プリセット** ボタンで代表的な系を一括設定：可観測系，不可観測系，可検出系．
- **計算・更新** ボタンを押すと，オブザーバゲインの計算・極の算出・時間応答シミュレーションが実行される．
- **s 平面プロット**：開ループ極とオブザーバ極 ( $A + LC$ の固有値) を表示．
- **時間応答プロット**：実状態 $x_1(t)$ , $x_2(t)$ と推定状態 $\hat{x}_1(t)$ , $\hat{x}_2(t)$ を重ねて表示．
- **推定誤差プロット**：推定誤差 $\varepsilon(t) = x(t) - \hat{x}(t)$ の各成分の時間変化を表示．
- **計算結果**：オブザーバゲイン $L$ ，オブザーバ極，可観測性行列の行列式 $\det(V_o)$ が表示される．

---

## オブザーバとは

### 状態推定の必要性

状態フィードバック制御 $u(t) = Kx(t)$ を実現するには，すべての状態変数 $x(t)$ が計測可能でなければなりません．しかし実際のシステムでは，センサで直接計測できるのは出力 $y(t) = Cx(t)$ のみであり，内部状態のすべてを知ることはできません．

**オブザーバ**(状態観測器) は，計測可能な入力 $u(t)$ と出力 $y(t)$ の情報から，計測できない内部状態 $x(t)$ を推定する動的システムです．

### オブザーバの構成

出力をもつ状態空間モデルを考えます．

```math
\dot{x}(t) = Ax(t) + Bu(t), \qquad y(t) = Cx(t)
```

ここで $x(t) \in \mathbb{R}^n$ は状態ベクトル， $u(t) \in \mathbb{R}$ は制御入力， $y(t) \in \mathbb{R}$ は出力， $C \in \mathbb{R}^{1 \times n}$ は出力行列です．

同一次元オブザーバは，実システムと同じ次元の推定モデルに出力の修正項を加えた以下の構造をもちます．

```math
\dot{\hat{x}}(t) = A\hat{x}(t) + Bu(t) - L\bigl(y(t) - \hat{y}(t)\bigr)
```

```math
\hat{y}(t) = C\hat{x}(t)
```

ここで $\hat{x}(t)$ は状態の推定値， $\hat{y}(t)$ は出力の推定値， $L \in \mathbb{R}^{n \times 1}$ はオブザーバゲインです．

### ブロック図の考え方

オブザーバは以下の2つの要素で構成されます．

1. **モデルベースの予測**： $A\hat{x} + Bu$ により，既知のシステムモデルと入力情報を使って状態を予測する．
2. **出力誤差による修正**：予測出力 $\hat{y} = C\hat{x}$ と実出力 $y = Cx$ の差 $y - \hat{y}$ にゲイン $L$ を掛けて予測を修正する．

出力誤差 $y - \hat{y}$ が大きいほど強い修正がかかるため，推定値は実状態に引き寄せられます．

---

## 誤差ダイナミクス

### 推定誤差の定義

推定誤差を次のように定義します．

```math
\varepsilon(t) = x(t) - \hat{x}(t)
```

### 誤差の微分方程式

実システムとオブザーバの微分方程式の差をとります．

```math
\dot{\varepsilon}(t) = \dot{x}(t) - \dot{\hat{x}}(t) = Ax + Bu - \bigl[A\hat{x} + Bu - L(y - \hat{y})\bigr]
```

$y - \hat{y} = Cx - C\hat{x} = C\varepsilon$ を代入すると：

```math
\dot{\varepsilon}(t) = A\varepsilon(t) - LC\varepsilon(t) = (A - LC)\,\varepsilon(t)
```

本シミュレータでは $-L$ の代わりに $L$ を直接用いる符号規約を採っているため，誤差ダイナミクスは次のように表されます．

```math
\dot{\varepsilon}(t) = (A + LC)\,\varepsilon(t)
```

### 誤差の収束条件

誤差の解は行列指数関数で表されます．

```math
\varepsilon(t) = e^{(A+LC)t}\,\varepsilon(0)
```

$A + LC$ のすべての固有値が複素左半面にあれば（実部が負であれば），任意の初期誤差 $\varepsilon(0)$ に対して $\varepsilon(t) \to 0$ ( $t \to \infty$ ) が保証されます．すなわち，**推定値 $\hat{x}(t)$ は時間とともに真の状態 $x(t)$ に収束**します．

$A + LC$ の固有値の実部が大きく負であるほど，収束は速くなります．

---

## 可観測性

### 可観測性の定義

システム $(A, C)$ が**可観測**であるとは，任意の有限時間区間 $[0, T]$ 上の出力 $y(t)$ と入力 $u(t)$ から，初期状態 $x(0)$ を一意に決定できることをいいます．

直感的には，出力の時間変化を観察するだけで内部状態を完全に復元できるという性質です．

### 可観測性行列

可観測性の判定には**可観測性行列** $V_o$ を用います．

```math
V_o = \begin{bmatrix} C \\ CA \\ CA^2 \\ \vdots \\ CA^{n-1} \end{bmatrix}
```

2次系 ( $n = 2$ ) の場合：

```math
V_o = \begin{bmatrix} C \\ CA \end{bmatrix} \in \mathbb{R}^{2 \times 2}
```

### 可観測性の判定条件

```math
\mathrm{rank}(V_o) = n \quad \Longleftrightarrow \quad \det(V_o) \neq 0 \quad \Longleftrightarrow \quad \text{系は可観測}
```

系が可観測であれば，オブザーバゲイン $L$ を適切に設計することにより $A + LC$ の固有値を任意の位置に配置でき，任意の速さで推定誤差を収束させることができます．

### 可観測性ペンシル

可観測性は固有値の観点からも理解できます．すべての複素数 $s$ に対して次の行列がフルランクであるとき，系は可観測です．

```math
\mathrm{rank}\begin{bmatrix} sI - A \\ C \end{bmatrix} = n \quad \text{(すべての } s \in \mathbb{C} \text{ に対して)}
```

この行列を**可観測性ペンシル**と呼びます．ランクが落ちる $s$ が存在する場合，その $s$ は不可観測な固有値(不可観測モード)に対応します．

### 可検出性

系が可観測でなくても，**不可観測な固有値がすべて安定**(実部が負)であれば，系は**可検出**(detectable)であるといいます．

可検出な系では，不可観測なモードの推定誤差は自然に減衰するため，オブザーバを設計することが可能です．ただし，不可観測モードに対応する固有値はゲイン $L$ で移動させることができないため，収束速度は制限されます．

| 性質 | 条件 | オブザーバ設計 |
|---|---|---|
| 可観測 | rank( $V_o$ ) $= n$ | 極を任意に配置可能 |
| 可検出 | 不可観測モードの実部 < 0 | 安定なオブザーバを設計可能（不可観測モードの極は固定） |
| 不可観測かつ不可検出 | 不安定な不可観測モードが存在 | 安定なオブザーバを設計不可 |

---

## オブザーバゲインの設計

### 双対システムによる極配置

オブザーバゲイン $L$ の設計は，状態フィードバックの極配置問題と**双対**の関係にあります．

$A + LC$ の固有値を配置する問題は， $(A^T, C^T)$ に対する状態フィードバック $K$ の極配置問題と等価です．

```math
A + LC \quad \longleftrightarrow \quad A^T + C^T K'
```

すなわち，双対システム $(A^T, C^T)$ に対して Ackermann の公式を適用し，得られた $K'$ を転置することでオブザーバゲインが求まります．

```math
L = (K')^T
```

### Ackermann の公式（双対系への適用）

双対系の可制御性行列を用いた Ackermann の公式：

```math
K' = -\begin{bmatrix} 0 & \cdots & 0 & 1 \end{bmatrix} W_d^{-1}\,\varphi_P(A^T)
```

ここで $W_d$ は双対系の可制御性行列です．

```math
W_d = \begin{bmatrix} C^T & A^T C^T & \cdots & (A^T)^{n-1} C^T \end{bmatrix}
```

$\varphi_P(A^T)$ は所望の特性多項式にシステム行列 $A^T$ を代入した行列です．

### オブザーバ極の選び方

オブザーバ極の配置には以下の指針があります．

- **状態フィードバック極よりも左に配置する**：オブザーバの収束が閉ループ応答よりも十分に速くなるようにします．一般的にはオブザーバ極の実部の絶対値を閉ループ極の 2 ~ 5 倍程度にします．
- **極を左に寄せすぎない**：過度に速い収束を要求すると，ゲイン $L$ が大きくなり，計測ノイズが増幅されます．
- **複素極は共役ペアで指定する**：ゲイン $L$ を実数とするために，所望の極が複素数の場合は必ず共役複素数ペアで指定します．

---

## 双対性

### 可制御性と可観測性の双対

状態フィードバックの設計 (極配置) とオブザーバの設計 (ゲイン設計) の間には**双対性**が成り立ちます．

| 状態フィードバック側 | オブザーバ側 |
|---|---|
| システム $(A, B)$ | 双対システム $(A^T, C^T)$ |
| 可制御性行列 $W_c$ | 可観測性行列 $V_o$ |
| 可制御 | 可観測 |
| 可安定 (stabilizable) | 可検出 (detectable) |
| フィードバックゲイン $K$ | オブザーバゲイン $L$ |
| 閉ループ行列 $A + BK$ | 誤差ダイナミクス行列 $A + LC$ |

この双対性により，状態フィードバックの極配置で培った設計手法をそのままオブザーバゲインの設計に転用できます．具体的には以下が成り立ちます．

- $(A, B)$ が**可制御** $\Longleftrightarrow$ $(A^T, B^T)$ が**可観測**
- $(A, C)$ が**可観測** $\Longleftrightarrow$ $(A^T, C^T)$ が**可制御**
- $(A, B)$ が**可安定** $\Longleftrightarrow$ $(A^T, B^T)$ が**可検出**

---

## プリセット例の解説

### 可観測系

可観測性行列 $V_o$ がフルランク ( $\det(V_o) \neq 0$ ) であるシステムです．オブザーバゲイン $L$ によって $A + LC$ の固有値を任意に配置でき，推定誤差を所望の速さで収束させることができます．

シミュレーション結果では，推定状態 $\hat{x}(t)$ が実状態 $x(t)$ に速やかに収束し，推定誤差 $\varepsilon(t) \to 0$ となることが確認できます．

### 不可観測系

可観測性行列のランクが不足 ( $\det(V_o) = 0$ ) しているシステムです．出力 $y(t)$ から内部状態を完全に復元することができません．

この場合，オブザーバゲイン $L$ をどのように選んでも $A + LC$ の固有値を自由に配置することができず，推定誤差が収束する保証がありません．シミュレーション結果では推定誤差が収束しない，または特定の方向の誤差が残留する様子が観察できます．

### 可検出系

系は可観測ではありませんが ( $\det(V_o) = 0$ )，不可観測な固有値の実部がすべて負であるため**可検出**です．

不可観測なモードは $L$ で極を移動させることはできませんが，もともと安定であるため誤差は自然に減衰します．可観測なモードに対してはゲイン $L$ で収束速度を調整できます．シミュレーション結果では，推定誤差 $\varepsilon(t) \to 0$ となりますが，不可観測モードに対応する成分は固有の減衰率でゆっくり収束する様子が確認できます．

---

## パラメータ一覧

### 入力パラメータ

| パラメータ | 説明 | 既定値 |
|---|---|---|
| $A$ | システム行列 (2x2) | プリセットにより異なる |
| $B$ | 入力行列 (2x1) | プリセットにより異なる |
| $C$ | 出力行列 (1x2) | プリセットにより異なる |
| $x_1(0)$ | 実システムの初期状態の第1成分 | プリセットにより異なる |
| $x_2(0)$ | 実システムの初期状態の第2成分 | プリセットにより異なる |
| $\hat{x}_1(0)$ | オブザーバ初期状態の第1成分 | プリセットにより異なる |
| $\hat{x}_2(0)$ | オブザーバ初期状態の第2成分 | プリセットにより異なる |
| $L$ | オブザーバゲイン (2x1) | プリセットにより異なる |
| $T$ | シミュレーション終了時間 [s] | -- |
| $dt$ | 時間刻み幅 [s] | -- |

### 計算結果（出力）

| 表示項目 | 説明 |
|---|---|
| オブザーバゲイン $L$ | 極配置で算出された $L = [l_1, l_2]^T$ |
| 開ループ極 | $A$ の固有値 |
| オブザーバ極 | $A + LC$ の固有値 |
| $\det(V_o)$ | 可観測性行列の行列式 ( $\neq 0$ で可観測) |

### プロット

| プロット | 内容 |
|---|---|
| s 平面 | 開ループ極とオブザーバ極 ( $A + LC$ の固有値) の表示 |
| 時間応答 | 実状態 $x_1(t)$ , $x_2(t)$ (実線) と推定状態 $\hat{x}_1(t)$ , $\hat{x}_2(t)$ (破線) |
| 推定誤差 | $\varepsilon_1(t) = x_1(t) - \hat{x}_1(t)$ , $\varepsilon_2(t) = x_2(t) - \hat{x}_2(t)$ の時間変化 |

### 数値計算手法

| 項目 | 手法 |
|---|---|
| ゲイン算出 | Ackermann の公式（双対系への適用） |
| 時間積分 | 4次 Runge-Kutta 法 (RK4) |
| 可観測性判定 | $\det(V_o)$ の計算 |

---

*この資料は付属の HTML シミュレータ (`observer_simulator.html`) の理論的背景と操作方法を記述したものです．*
