# 強化学習 — ロボット迷路探索

Q学習（Q-Learning）アルゴリズムを用いて，ロボットが迷路の中でスタートからゴールまでの最適経路を自律的に発見するシミュレータの解説です．

> **注意**：本シミュレータは制御工学の講義資料ではなく，強化学習の基礎概念を体験するための補助教材です．エージェントが試行錯誤を通じて環境を学習し，最適な行動方策を獲得していく過程を視覚的に観察できます．

---

## デモ / 使い方

付属の `rl_maze.html` を **ブラウザで開くだけ**で動作します（外部ライブラリ不要・オフライン可）．

```bash
git clone https://github.com/knjsawada-misclab/Control-study.git
cd Control-study
# rl_maze.html をブラウザで開く（ダブルクリック or 下記）
open rl_maze.html        # macOS
# xdg-open rl_maze.html  # Linux
# start rl_maze.html     # Windows
```

操作のポイント：

- **▶ 学習開始** ボタンで Q 学習を開始．学習中は **⏸ 一時停止** ボタンに切り替わり，いつでも中断・再開が可能．
- **⟳ リセット** ボタンで Q テーブル・統計・グラフをすべて初期化し，最初からやり直せる．
- **学習率 α** スライダー（0.01〜1.0，既定 0.1）で学習の更新幅を調整．
- **割引率 γ** スライダー（0.1〜0.99，既定 0.95）で将来報酬の重みを変更．
- **速度** スライダー（1〜5，既定 3＝中）で学習の描画速度を切り替え．
- 画面上部の **可視化モード**（Q値アロー／価値ヒートマップ／最適方策）を切り替えて，学習の進行をさまざまな角度から観察．
- 画面右側にはエピソードごとの**報酬の推移**グラフがリアルタイムで表示される．

---

## 強化学習とは

強化学習（Reinforcement Learning）は，**エージェント**（ここではロボット）が**環境**（迷路）の中で行動し，受け取る**報酬**を手がかりに，最適な**方策**（行動の決め方）を自ら獲得する機械学習の枠組みです．

```text
   ┌─────────┐    行動 a     ┌─────────┐
   │         │─────────────▶│         │
   │ エージェント │              │   環境   │
   │ (ロボット) │◀─────────────│  (迷路)  │
   └─────────┘  状態 s', 報酬 r └─────────┘
```

- **状態 (state)**：エージェントが置かれている状況（＝迷路上のセル座標）．
- **行動 (action)**：エージェントが選択できる操作（＝上下左右の移動）．
- **報酬 (reward)**：行動の結果として環境から得られるスカラー値．ゴールに到達すれば大きな正の報酬，壁にぶつかれば負の報酬を受ける．
- **方策 (policy)**：各状態でどの行動を選ぶかの規則．強化学習の目標は累積報酬を最大化する方策を見つけること．

教師あり学習と異なり，正解の行動が事前に与えられるのではなく，エージェント自身が**試行錯誤（trial and error）**を通じて発見する点が特徴です．

---

## Q学習アルゴリズム

Q学習は，状態-行動ペアの価値を表す **Q値** $Q(s, a)$ を繰り返し更新することで最適方策を学習する，代表的なモデルフリー強化学習アルゴリズムです．

### Q値の更新式

```math
Q(s, a) \;\leftarrow\; Q(s, a) + \alpha \left[ r + \gamma \max_{a'} Q(s', a') - Q(s, a) \right]
```

ここで：

| 記号 | 意味 |
|------|------|
| $s$ | 現在の状態 |
| $a$ | 実行した行動 |
| $r$ | 受け取った報酬 |
| $s'$ | 行動後の遷移先状態 |
| $\alpha$ | 学習率（更新の大きさ） |
| $\gamma$ | 割引率（将来報酬の重み） |

大括弧の中身 $r + \gamma \max_{a'} Q(s', a') - Q(s, a)$ は**TD誤差（Temporal Difference error）**と呼ばれ，現在の Q 値の推定と，報酬＋遷移先の最良の推定値との差を表します．この差をゼロに近づけるように Q 値を修正していくのが Q 学習の本質です．

### ε-greedy 探索

学習中は**探索（exploration）**と**活用（exploitation）**のバランスが重要です．本シミュレータでは ε-greedy 方策を用います．

- 確率 $\varepsilon$ でランダムな行動を選択（**探索**）
- 確率 $1 - \varepsilon$ で現在の Q 値が最大の行動を選択（**活用**）

$\varepsilon$ はエピソードごとに減衰します．

```math
\varepsilon \;\leftarrow\; \max(0.01,\;\varepsilon \times 0.997)
```

初期値 $\varepsilon = 1.0$ (完全ランダム) から始まり，学習が進むにつれて活用の比率が増え，最終的に $\varepsilon = 0.01$ に収束します．これにより，序盤は未知の経路を広く探索し，終盤は学習済みの知識を活かした行動が主体となります．

---

## 迷路環境の設定

### グリッド構造

迷路は **10行 × 12列** のグリッドで構成されています．各セルは通路（0）または壁（1）のいずれかです．

| 要素 | 位置 / 説明 | 表示色 |
|------|-------------|--------|
| 🤖 ロボット（エージェント） | 学習中に移動 | 黄色 |
| 🏁 ゴール | 行 8・列 10（右下付近） | 緑 |
| 🚀 スタート | 行 1・列 1（左上付近） | 青 |
| 🧱 壁 | 迷路の障害物 | 暗灰色 |

### 状態と行動

- **状態**：セルの座標 $(r, c)$．通路セルの数だけ状態が存在する．
- **行動**：上（↑）・下（↓）・左（←）・右（→）の4方向．

### 報酬設計

| 状況 | 報酬 |
|------|------|
| ゴールに到達 | $+100$ |
| 壁に衝突（移動失敗） | $-1$ |
| 通常の移動 | $-0.1$ |

通常の移動に小さな負の報酬 $-0.1$ を与えることで，エージェントはなるべく少ないステップでゴールに到達する経路を選好するようになります．壁への衝突に $-1$ のペナルティを課すことで，壁に向かう行動を早期に回避するよう学習します．

### エピソードの終了条件

各エピソード（1回の試行）は，以下のいずれかで終了します．

- ゴールに到達した場合
- 500 ステップを超えた場合（タイムアウト）

---

## ハイパーパラメータの役割

### 学習率 α（0.01〜1.0，既定 0.1）

Q 値の更新幅を決めます．

- **α が大きい**：新しい経験を重視し，Q 値が素早く変化する．ただし過去の学習結果を上書きしやすく，値が振動して収束しにくい場合がある．
- **α が小さい**：過去の経験を保持しつつ緩やかに更新する．安定だが収束に時間がかかる．

### 割引率 γ（0.1〜0.99，既定 0.95）

将来の報酬をどの程度重視するかを決めます．

- **γ が 1 に近い**：遠い将来の報酬まで考慮し，長期的に最適な経路を学ぶ．
- **γ が小さい**：目先の報酬を重視する近視眼的な方策になり，ゴールまでの最適経路を見つけにくくなる場合がある．

迷路探索では，ゴール到達の報酬が多くのステップ先にあるため，$\gamma$ を十分大きく（0.9 以上）設定することが重要です．

### 探索率 ε

直接スライダーでは操作しませんが，学習の進行に伴い自動的に減衰します．

```math
\varepsilon_0 = 1.0 \quad\longrightarrow\quad \varepsilon \times 0.997 \;/\;\text{エピソード} \quad\longrightarrow\quad \varepsilon_{\min} = 0.01
```

- **序盤（ε ≈ 1.0）**：ほぼランダムに動き回り，迷路全体の構造を探索．
- **中盤（ε ≈ 0.3〜0.5）**：学習した知識を活用しつつ，まだ新しい経路も試す．
- **終盤（ε → 0.01）**：ほぼ最適と推定される行動を選択し，経路が安定する．

### 速度（1〜5，既定 3）

学習の描画速度を調整します．内容（学習アルゴリズム自体）には影響しません．速度を上げると短時間で多くのエピソードを実行できます．

---

## 可視化モードの見方

シミュレータには3つの可視化モードがあり，学習状況を異なる観点から確認できます．

### Q値アロー

各セルに**4方向の矢印**が表示されます．矢印の**長さと明るさ**が，その方向の Q 値の大きさに比例します．

- ゴールに近いセルほど矢印が長く・明るくなる．
- 学習初期はすべての矢印がほぼ均一（Q 値がゼロに近い）．
- 学習が進むと，ゴールへ向かう方向の矢印が卓越してくる．
- 壁方向の矢印は負の Q 値を持つため短い（または表示されない）．

### 価値ヒートマップ

各セルの**最大 Q 値** $\max_a Q(s, a)$ を色の濃さで表示します．暗い色から赤へのグラデーションで，Q 値の高いセル（＝ゴールに近く価値の高い状態）が一目で分かります．

- ゴール付近が最も赤く，スタート付近は暗い（＝ゴールまで多くのステップが必要で割引が効くため）．
- 学習が進むにつれ，ゴールから逆伝播するように色が広がっていく様子が観察できる．

### 最適方策

各セルに**1つの矢印**のみを表示し，現在の Q 値に基づく**貪欲方策**（greedy policy）を示します．

```math
\pi(s) = \arg\max_a Q(s, a)
```

- 学習が十分に進めば，スタートからゴールまでの矢印を辿ることで最適経路が視覚化される．
- 学習初期は方策が不安定で矢印の方向が頻繁に変わる．

---

## 学習の観察ポイント

学習を実行しながら，以下の点に注目すると強化学習の理解が深まります．

### 1. 序盤：ランダム探索（ε ≈ 1.0）

- ロボットはほぼランダムに動き回り，ゴールにたどり着くまで非常に多くのステップを要する．
- 報酬グラフは大きな負の値（ステップ数 × $-0.1$ ＋壁衝突ペナルティ）を示す．
- Q値アローモードでは矢印がほぼ均一で，学習がまだ進んでいないことが分かる．

### 2. 中盤：経路の発見と改善

- ゴール到達の経験が蓄積されるにつれ，Q 値がゴールから逆方向へ伝播していく．
- 価値ヒートマップでゴール周辺から徐々に色が広がる様子が観察できる．
- 報酬グラフが上昇し始め，ステップ数が減少する．
- まだ探索率が残っているため，時折ランダムな行動で報酬が落ち込む．

### 3. 終盤：方策の収束（ε → 0.01）

- 最適方策モードでスタートからゴールまでの一貫した経路が矢印で示される．
- 報酬グラフがほぼ一定の高い値に安定する（＝毎回ほぼ同じ短い経路でゴールに到達）．
- 探索率表示が 0.01 に近づき，学習済みの方策に従った行動が支配的になる．

### 4. ハイパーパラメータを変えて比較

- **α を大きくする（例：0.5）**：収束は速いが Q 値が振動しやすい．
- **α を小さくする（例：0.01）**：安定だが収束に非常に多くのエピソードが必要．
- **γ を小さくする（例：0.5）**：ゴール付近のみ学習が進み，遠いセルの方策が不安定になる．
- リセットして条件を変えながら繰り返し実験し，パラメータの影響を体感することが学習の目的です．

---

*この資料は付属の HTML シミュレータ（`rl_maze.html`）の学習アルゴリズムと操作方法を記述したものです．*
