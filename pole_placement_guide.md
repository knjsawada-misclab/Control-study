# 状態フィードバックによる極配置シミュレータ

制御工学（応用理工学科機械工学科目3年）／大阪大学 澤田賢治

状態フィードバック $u(t) = Kx(t)$ による極配置法のインタラクティブシミュレータです．システム行列 $(A, B)$ と目標極を設定すると，Ackermann の公式でフィードバックゲイン $K$ を算出し，開ループ・閉ループの極と時間応答を同時に可視化します．

> **対象**：線形時不変の状態空間モデル $\dot{x} = Ax + Bu$（2次系）に対して，状態フィードバックで閉ループ極を任意に配置する設計法を体験的に学ぶシミュレータです．

---

## デモ / 使い方

付属の `pole_placement.html` を **ブラウザで開くだけ**で動作します（外部ライブラリ不要・オフライン可）．

```bash
git clone https://github.com/knjsawada-misclab/Control-study.git
cd Control-study
# pole_placement.html をブラウザで開く（ダブルクリック or 下記）
open pole_placement.html        # macOS
# xdg-open pole_placement.html  # Linux
# start pole_placement.html     # Windows
```

操作のポイント：

- **システム行列** $A$（2×2），$B$（2×1）の各要素を入力欄で編集．
- **初期状態** $x_0 = (x_1(0),\; x_2(0))$ を設定．
- **目標極** を共役複素数ペア $\sigma \pm j\omega$ で指定（$\sigma$：実部，$\omega$：虚部）．
- **シミュレーション条件**：終了時間 $T$ [s] とステップ幅 $dt$ を調整可能．
- **▶ 計算・更新** ボタンを押すと，ゲイン $K$ の計算・極の算出・時間応答シミュレーションが実行される．
- **プリセット** ボタンで代表的な系を一括設定：単振動子（質量-バネ），不安定系，二重積分器，倒立振子（線形化）．
- **s 平面プロット**：開ループ極（×），閉ループ極（●），目標極（◎），減衰比 $\zeta$ の等高線を表示．
- **時間応答プロット**：$x_1(t)$，$x_2(t)$ の開ループ応答と閉ループ応答を重ねて表示．
- **計算結果**：フィードバックゲイン $K$，開ループ極 $(\lambda_1, \lambda_2)$，閉ループ極 $(\lambda_1, \lambda_2)$，固有角周波数 $\omega_n$，減衰比 $\zeta$，可制御性行列の行列式 $\det(W_c)$ が表示される．

---

## 状態フィードバック制御

### 状態空間モデル

プラントの状態空間表現を次のように置きます．

```math
\dot{x}(t) = Ax(t) + Bu(t)
```

ここで $x(t) \in \mathbb{R}^n$ は状態ベクトル，$u(t) \in \mathbb{R}$ はスカラーの制御入力，$A \in \mathbb{R}^{n \times n}$ はシステム行列，$B \in \mathbb{R}^{n \times 1}$ は入力行列です．本シミュレータでは $n = 2$ の2次系を扱います．

### 状態フィードバック則

全状態量が計測可能であるとき，制御入力を状態の線形結合で与えます．

```math
u(t) = Kx(t), \qquad K = \begin{bmatrix} k_1 & k_2 & \cdots & k_n \end{bmatrix} \in \mathbb{R}^{1 \times n}
```

これを状態方程式に代入すると，閉ループ系が得られます．

```math
\dot{x}(t) = (A + BK)\,x(t)
```

閉ループ系の解は行列指数関数で表され，初期状態 $x(0) = x_0$ からの自由応答となります．

```math
x(t) = e^{(A+BK)t}\,x_0
```

閉ループ系の安定性と応答特性は，行列 $A + BK$ の固有値（閉ループ極）によって決まります．**極配置法は，この固有値を所望の位置に配置するように $K$ を設計する手法**です．

---

## 極配置法

### 設計手順

状態フィードバックゲイン $K$ を求めて閉ループ極を所望の位置に配置する手順は次のとおりです．

**ステップ 1：所望の閉ループ極 $P_1, P_2, \dots, P_n$ を選ぶ**

応答仕様（速応性・減衰・振動の有無）から，$A + BK$ の固有値として望む値を決めます．

**ステップ 2：所望の特性多項式を組み立てる**

```math
\varphi_P(s) = (s - P_1)(s - P_2) \cdots (s - P_n)
```

**ステップ 3：$A + BK$ の特性多項式と係数比較する**

```math
\varphi_{A+BK}(s) = \det\!\bigl(sI - (A + BK)\bigr)
```

$\varphi_{A+BK}(s) = \varphi_P(s)$ となるように $K$ の各成分を連立方程式で求めます．

### Ackermann の公式

本シミュレータでは Ackermann の公式を用いてゲイン $K$ を計算します．可制御性行列 $W_c$ とシステム行列 $A$ を用いて，$K$ を直接求める公式です．

```math
K = -\begin{bmatrix} 0 & \cdots & 0 & 1 \end{bmatrix} W_c^{-1}\,\varphi_P(A)
```

ここで $\varphi_P(A)$ は所望の特性多項式にシステム行列 $A$ を代入した行列です．$W_c^{-1}$ が存在するためには系が**可制御**でなければなりません．

### 複素極の制約

フィードバックゲイン $K$ を実数とするためには，所望の極が複素数の場合，**必ず共役複素数ペア**で指定する必要があります．本シミュレータでは目標極を $\sigma \pm j\omega$ の形で指定するため，この制約は自動的に満たされます．

---

## 2次系の極と応答特性

2次系（$n = 2$）の場合，所望の特性多項式は次の標準形で書けます．

```math
\varphi_P(s) = s^2 + 2\zeta\omega_n s + \omega_n^2
```

ここで $\omega_n$ は固有角周波数，$\zeta$ は減衰比です．目標極 $\sigma \pm j\omega$ との関係は次のとおりです．

```math
\omega_n = \sqrt{\sigma^2 + \omega^2}, \qquad \zeta = \frac{-\sigma}{\omega_n} = \frac{-\sigma}{\sqrt{\sigma^2 + \omega^2}}
```

### 減衰比と極の配置

| 減衰比 $\zeta$ の範囲 | 極の種類 | 応答の特徴 |
|---|---|---|
| $0 < \zeta < 1$ | 共役複素数（虚部あり） | **不足減衰**：振動しながら収束 |
| $\zeta = 1$ | 重実根 | **臨界減衰**：振動なしで最速収束 |
| $\zeta > 1$ | 異なる2実根 | **過減衰**：振動なしだが収束が遅い |
| $\zeta = 1/\sqrt{2} \approx 0.707$ | 実部と虚部の絶対値が等しい | **Butterworth 標準形**：平坦な周波数特性 |

### 極の位置と応答の関係

- **実部** $\sigma$（＜ 0 で安定）：$|\sigma|$ が大きいほど**収束が速い**．
- **虚部** $\omega$：$|\omega|$ が大きいほど**振動が激しい**．
- s 平面で極が左半面にあれば安定，右半面にあれば不安定です．
- 原点から遠いほど応答速度が速く，実軸からの角度が大きいほど振動的になります．

---

## 可制御性

任意の極配置が可能であるための**必要十分条件**は，系が**可制御**であることです．

### 可制御性行列

```math
W_c = \begin{bmatrix} B & AB & A^2B & \cdots & A^{n-1}B \end{bmatrix}
```

2次系（$n = 2$）の場合：

```math
W_c = \begin{bmatrix} B & AB \end{bmatrix} \in \mathbb{R}^{2 \times 2}
```

### 可制御性の判定

```math
\text{rank}(W_c) = n \quad \Longleftrightarrow \quad \det(W_c) \neq 0 \quad \Longleftrightarrow \quad \text{系は可制御}
```

$\det(W_c) = 0$ の場合，Ackermann の公式で $W_c^{-1}$ が計算できず，極を自由に配置することができません．シミュレータでは $\det(W_c)$ の値を表示し，可制御性を確認できます．

---

## プリセット例の解説

### 単振動子（質量-バネ）

質量 $m$ がバネ定数 $k$ のバネで支えられた系です．

```math
m\ddot{z} = -kz + u
```

状態変数 $x = \begin{bmatrix} z \\ \dot{z} \end{bmatrix}$ として状態空間表現にすると：

```math
A = \begin{bmatrix} 0 & 1 \\ -k/m & 0 \end{bmatrix}, \qquad B = \begin{bmatrix} 0 \\ 1/m \end{bmatrix}
```

開ループでは減衰がないため純虚数極をもち，永続的に振動します．状態フィードバックで極を左半面の安定な位置に移動させることで，振動を減衰させることができます．

### 不安定系

開ループ極が右半面にある不安定なシステムです．制御入力なしでは状態が発散しますが，適切なフィードバックゲイン $K$ によって極を左半面に移動させ，安定化できます．

### 二重積分器

```math
\ddot{z} = u
```

```math
A = \begin{bmatrix} 0 & 1 \\ 0 & 0 \end{bmatrix}, \qquad B = \begin{bmatrix} 0 \\ 1 \end{bmatrix}
```

開ループ極が原点に2つあり（$\lambda_1 = \lambda_2 = 0$），入力がなければ状態が一定速度で発散します．PD 制御 $u = -k_p z - k_d \dot{z}$ は状態フィードバック $K = \begin{bmatrix} -k_p & -k_d \end{bmatrix}$ と等価であり，閉ループ極の位置で $k_p$，$k_d$ が決定されます．

### 倒立振子（線形化）

鉛直上向きの平衡点まわりで線形化した倒立振子モデルです．開ループでは実軸上に正の極をもつ不安定系です．状態フィードバックで安定な極を配置することにより，鉛直姿勢を保つことができます．

---

## パラメータ一覧

### 入力パラメータ

| パラメータ | 説明 | 既定値 |
|---|---|---|
| $A$ | システム行列（2×2） | プリセットにより異なる |
| $B$ | 入力行列（2×1） | プリセットにより異なる |
| $x_1(0)$ | 初期状態の第1成分 | プリセットにより異なる |
| $x_2(0)$ | 初期状態の第2成分 | プリセットにより異なる |
| $\sigma$ | 目標極の実部 | プリセットにより異なる |
| $\omega$ | 目標極の虚部 | プリセットにより異なる |
| $T$ | シミュレーション終了時間 [s] | — |
| $dt$ | 時間刻み幅 [s] | — |

### 計算結果（出力）

| 表示項目 | 説明 |
|---|---|
| フィードバックゲイン $K$ | Ackermann の公式で算出された $K = \begin{bmatrix} k_1 & k_2 \end{bmatrix}$ |
| 開ループ極 $(\lambda_1, \lambda_2)$ | $A$ の固有値 |
| 閉ループ極 $(\lambda_1, \lambda_2)$ | $A + BK$ の固有値 |
| $\omega_n$ | 閉ループ系の固有角周波数 |
| $\zeta$ | 閉ループ系の減衰比 |
| $\det(W_c)$ | 可制御性行列の行列式（$\neq 0$ で可制御） |

### プロット

| プロット | 内容 |
|---|---|
| s 平面 | 開ループ極（×），閉ループ極（●），目標極（◎），減衰比 $\zeta$ の等高線 |
| 時間応答 | $x_1(t)$，$x_2(t)$ の開ループ応答（破線）と閉ループ応答（実線） |

### 数値計算手法

| 項目 | 手法 |
|---|---|
| ゲイン算出 | Ackermann の公式 |
| 時間積分 | 4次 Runge-Kutta 法（RK4） |
| 可制御性判定 | $\det(W_c)$ の計算 |

---

*この資料は付属の HTML シミュレータ（`pole_placement.html`）の理論的背景と操作方法を記述したものです．*
