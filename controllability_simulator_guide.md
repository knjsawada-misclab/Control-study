# 可制御性シミュレータ

制御工学（応用理工学科機械工学科目3年）／大阪大学 澤田賢治

状態フィードバック $u(t) = Kx(t)$ による極配置法において，**可制御性**が極配置の可否を決定することをインタラクティブに体験するシミュレータです．システム行列 $(A, B)$ と目標極を設定すると，可制御性行列・可制御性ペンシルによる解析，特性多項式の因数分解，フィードバックゲイン $K$ の算出を自動で行い，結果を可視化します．

> **対象**：線形時不変の状態空間モデル $\dot{x} = Ax + Bu$ (2次系) に対して，可制御・不可制御・可安定の各概念を体験的に学ぶシミュレータです．

---

## デモ / 使い方

付属の `controllability_simulator.html` を **ブラウザで開くだけ**で動作します（外部ライブラリ不要・オフライン可）．

```bash
git clone https://github.com/knjsawada-misclab/Control-study.git
cd Control-study
# controllability_simulator.html をブラウザで開く
start controllability_simulator.html     # Windows
# open controllability_simulator.html    # macOS
# xdg-open controllability_simulator.html # Linux
```

操作のポイント：

- **プリセット** で講義の問題1〜6に対応するシステムを一括設定．
- **システム行列** $A$ (2×2), $B$ (2×1) の各要素を入力欄で編集．
- **目標極** $P_1, P_2$ を実数で指定．
- **初期状態** $x_0 = (x_1(0),\; x_2(0))$ を設定．
- **▶ 計算・更新** ボタンで解析・シミュレーションを実行．
- **s 平面プロット** で開ループ極の可制御/不可制御を色分け表示．
- **時間応答プロット** で開ループと閉ループの応答を比較．

---

## 可制御性とは

### 定義

システム $\dot{x}(t) = Ax(t) + Bu(t)$ が**可制御**とは，任意の初期状態 $x_0 \in \mathbb{R}^n$ に対して，ある定数 $t_f > 0$ と入力 $u(t)$ , $t \in [0, t_f]$ が存在して，

```math
x(t_f) = e^{At_f} x_0 + \int_0^{t_f} e^{A(t_f - \tau)} Bu(\tau)\, d\tau = 0
```

を満たすようにできることです．可制御であれば，入力により任意の初期状態から有限時間で状態を零に持っていくことができます．

### 可制御性の条件

行列 $A \in \mathbb{R}^{n \times n}$ と行列 $B \in \mathbb{R}^{n \times m}$ に対して， $(A, B)$ が可制御であることは，以下の条件とそれぞれ等価です．

**条件① 極配置の自由度**

任意の極 $P_1, P_2, \dots, P_n \in \mathbb{C}$ に対して，状態フィードバック $u = Kx$ による極配置が可能です．

**条件② 可制御性行列**

```math
V_c := \begin{bmatrix} B & AB & A^2B & \cdots & A^{n-1}B \end{bmatrix} \in \mathbb{R}^{n \times nm}
```

が行フルランクであること（ $m = 1$ のとき $\det(V_c) \neq 0$ ）．本シミュレータではこの判定を行います．

**条件③ 可制御性ペンシル**

```math
P_c(s) := \begin{bmatrix} sI - A \mid B \end{bmatrix} \in \mathbb{R}^{n \times (n+m)}
```

が任意の $s \in \mathbb{C}$ に対して行フルランクであること．行フルランクでなくなる $s$ が**不可制御極**です．

**条件④ 可制御性グラミアン**

```math
G_c(t) := \int_0^t e^{-A\tau} B (e^{-A\tau} B)^\top d\tau
```

が $t > 0$ に対して正則となること．

---

## 極配置と可制御性

### 状態フィードバックによる極配置

状態方程式 $\dot{x} = Ax + Bu$ に対して $u(t) = Kx(t)$ と状態フィードバックを適用すると，閉ループ系は

```math
\dot{x}(t) = (A + BK)x(t)
```

となります．閉ループ系の特性多項式は

```math
\varphi_{A+BK}(s) = \det\!\bigl(sI - (A + BK)\bigr)
```

です．極配置法では，この特性多項式が所望の特性多項式

```math
\phi_P(s) = (s - P_1)(s - P_2) \cdots (s - P_n)
```

と一致するように $K$ を決定します．

### 可制御系での極配置（問題1）

$A = \begin{bmatrix} 0 & 1 \\ -3 & 4 \end{bmatrix}$ , $B = \begin{bmatrix} 0 \\ 1 \end{bmatrix}$ の場合：

```math
V_c = \begin{bmatrix} 0 & 1 \\ 1 & 4 \end{bmatrix}, \quad \det(V_c) = -1 \neq 0 \quad \Rightarrow \quad \text{可制御}
```

特性多項式は

```math
\varphi_{A+BK}(s) = s^2 - (4 + k_2)s + (3 - k_1)
```

であり，両方の係数が $k_1, k_2$ で自由に調整できます．例えば $P_1 = P_2 = -1$ のとき $k_1 = 2, k_2 = -6$ と求まります．

### 不可制御系での極配置（問題2）

$A = \begin{bmatrix} 1 & 0 \\ -3 & 4 \end{bmatrix}$ , $B = \begin{bmatrix} 0 \\ 1 \end{bmatrix}$ の場合：

```math
V_c = \begin{bmatrix} 0 & 0 \\ 1 & 4 \end{bmatrix}, \quad \det(V_c) = 0 \quad \Rightarrow \quad \text{不可制御}
```

特性多項式は

```math
\varphi_{A+BK}(s) = (s - 1)(s - 4 - k_2)
```

と因数分解されます．因子 $(s - 1)$ は $K$ に依存しない**固定因子**であり，**不可制御極** $s = 1$ に対応します．極 $s = 4 + k_2$ のみが $k_2$ で調整可能ですが，固定極 $s = 1$ を移動させることはできないため，任意の極配置は不可能です．

---

## 不可制御極と可安定性

### 不可制御極

$(A, B)$ が不可制御の場合，ある $s \in \mathbb{C}$ が存在して可制御性ペンシル $P_c(s) = [sI - A \mid B]$ のランクが $n$ 未満になります．そのような $s$ を**不可制御極**または**不可制御モード**と呼びます．不可制御極は状態フィードバックで移動させることができない固定極です．

### 可安定性

不可制御であっても，不可制御極がすべて安定（ $\mathrm{Re}[s] < 0$ ）であれば，閉ループ系を安定化することが可能です．この性質を**可安定**と呼びます．

```math
\text{可安定} \quad \Longleftrightarrow \quad \mathrm{Re}[s] > 0 \text{ を満たす任意の } s \text{ に対して } \mathrm{rank}\,P_c(s) = n
```

| 分類 | 条件 | 極配置の自由度 |
|---|---|---|
| **可制御** | 全ての極が可制御極 | 任意の極配置が可能 |
| **不可制御（可安定）** | 不可制御極がすべて安定 | 可制御極のみ配置可能，系の安定化は可能 |
| **不可制御（不可安定）** | 不安定な不可制御極が存在 | 系の安定化が不可能 |

---

## 可制御正準系

行列 $A$ , $B$ が次の形をしているとき，**可制御正準系**と呼びます．

```math
A = \begin{bmatrix} 0 & 1 & 0 & \cdots & 0 \\ 0 & 0 & 1 & \cdots & 0 \\ \vdots & \vdots & & \ddots & \vdots \\ 0 & 0 & \cdots & 0 & 1 \\ -a_0 & -a_1 & \cdots & -a_{n-2} & -a_{n-1} \end{bmatrix}, \quad B = \begin{bmatrix} 0 \\ 0 \\ \vdots \\ 0 \\ 1 \end{bmatrix}
```

可制御正準系では，閉ループ特性多項式の全ての係数が状態フィードバックゲインで独立に調整可能であり，可制御性行列は下三角行列で正則になります．プリセット「問題5-1」がこの形です．

---

## プリセット例の解説

### 問題1：可制御系

$A = \begin{bmatrix} 0 & 1 \\ -3 & 4 \end{bmatrix}$ , $B = \begin{bmatrix} 0 \\ 1 \end{bmatrix}$

$\det(V_c) = -1 \neq 0$ で可制御．任意の極を配置可能です．物理的には，機関車が貨車を牽引する系で，入力が $x_2$ の方程式に直接作用し，$A$ の非対角成分 $a_{12} = 1$ を通じて $x_1$ にも影響が伝わります．

### 問題2：不可制御（不可安定）

$A = \begin{bmatrix} 1 & 0 \\ -3 & 4 \end{bmatrix}$ , $B = \begin{bmatrix} 0 \\ 1 \end{bmatrix}$

$\det(V_c) = 0$ で不可制御．不可制御極 $s = 1$ は $\mathrm{Re}[s] > 0$ で不安定のため，不可安定です．特性多項式 $\varphi_{A+BK}(s) = (s-1)(s-4-k_2)$ から，極 $s = 1$ は制御入力で変更できないことがわかります．物理的には，貨車が自由に坂を下り，機関車ではその動きを制御できない状況です．

### 問題3：不可制御（可安定）

$A = \begin{bmatrix} -1 & 0 \\ -3 & -4 \end{bmatrix}$ , $B = \begin{bmatrix} 0 \\ 1 \end{bmatrix}$

$\det(V_c) = 0$ で不可制御ですが，不可制御極 $s = -1$ は安定であるため可安定です．特性多項式は $\varphi_{A+BK}(s) = (s+1)(s+4-k_2)$ となり，極 $s = -1$ は固定ですが安定なため，残りの可制御極を安定に配置すれば閉ループ全体を安定化できます．物理的には，貨車は摩擦で自然に止まり，機関車は自身の速度を制御する状況です．

### 問題4：可安定（重複固有値）

$A = \begin{bmatrix} -1 & 0 \\ 0 & -1 \end{bmatrix}$ , $B = \begin{bmatrix} 1 \\ 1 \end{bmatrix}$

重複固有値 $\lambda_1 = \lambda_2 = -1$ をもつ系です．$\det(V_c) = 0$ で不可制御ですが，不可制御極 $s = -1$ は安定なので可安定です．物理的には，2台の車両を同じアクセル・ブレーキで制御する状況で，車間距離は自由に制御できませんが，両車とも自然に減速するため安定です．

### 問題5-1：可制御正準系

$A = \begin{bmatrix} 0 & 1 \\ 0 & 0 \end{bmatrix}$ , $B = \begin{bmatrix} 0 \\ 1 \end{bmatrix}$

二重積分器であり，可制御正準系の形をしています．可制御で，PD 制御 $u = -k_p z - k_d \dot{z}$ による位置決めが可能です．移動体を任意の位置に制御できる可到達性を体験できます．

### 問題5-2：不可制御系

$A = \begin{bmatrix} 0 & 1 \\ 0 & 0 \end{bmatrix}$ , $B = \begin{bmatrix} 1 \\ 0 \end{bmatrix}$

同じ $A$ 行列でも $B$ が異なると不可制御になる例です．入力が $x_1$ にのみ作用し，$x_2$ には影響を与えられないため，$x_2$ が自由に変化してしまいます．

---

## パラメータ一覧

### 入力パラメータ

| パラメータ | 説明 | 既定値 |
|---|---|---|
| $A$ | システム行列（2×2） | プリセットにより異なる |
| $B$ | 入力行列（2×1） | プリセットにより異なる |
| $P_1, P_2$ | 目標極（実数） | $-1, -1$ |
| $x_1(0), x_2(0)$ | 初期状態 | $1, 0$ |
| $T$ | シミュレーション終了時間 [s] | $5$ |
| $dt$ | 時間刻み幅 [s] | $0.01$ |

### 解析結果（出力）

| 表示項目 | 説明 |
|---|---|
| 分類バッジ | 可制御 / 不可制御（可安定）/ 不可制御（不可安定） |
| 可制御性行列 $V_c$ | $V_c = [B, AB]$ とその行列式 |
| 固有値と可制御性ペンシル | 各固有値が可制御極か不可制御極かを判定 |
| 特性多項式 $\varphi_{A+BK}(s)$ | $K$ を含む閉ループ特性多項式（不可制御系では因数分解表示） |
| フィードバックゲイン $K$ | 極配置の解（解無しの場合はその旨を表示） |
| 閉ループ極 | $A + BK$ の固有値（ $K$ が存在する場合のみ） |

### プロット

| プロット | 内容 |
|---|---|
| s 平面 | 可制御極（緑×），不可制御極（赤×），目標極（青◎），閉ループ極（橙●） |
| 時間応答 | $x_1(t)$ , $x_2(t)$ の開ループ応答（薄色）と閉ループ応答（実線） |

### 数値計算手法

| 項目 | 手法 |
|---|---|
| 可制御性判定 | 可制御性行列の $\det(V_c)$ 計算と可制御性ペンシルのランク判定 |
| ゲイン算出 | 特性多項式の係数比較による連立方程式の求解 |
| 時間積分 | 4次 Runge-Kutta 法（RK4） |

---

*この資料は付属の HTML シミュレータ（`controllability_simulator.html`）の理論的背景と操作方法を記述したものです．*
