# 非線形システムの線形近似シミュレータ

制御工学 (応用理工学科機械工学科目3年) ／大阪大学 澤田賢治

非線形システムの**平衡点まわりの線形近似** (ヤコビ線形化) をインタラクティブに体験するシミュレータです．振り子を題材に，非線形システムと線形近似システムの応答を同時にシミュレーションし，アニメーション・位相平面・時間応答で比較します．

> **対象**：非線形状態方程式 $\dot{x} = f(x, u)$ の平衡点まわりの線形近似，下死点・上死点での安定性の違い，状態フィードバックによる安定化を体験的に学ぶシミュレータです．

---

## デモ / 使い方

付属の `linearization_simulator.html` を **ブラウザで開くだけ**で動作します (外部ライブラリ不要・オフライン可)．

```bash
git clone https://github.com/knjsawada-misclab/Control-study.git
cd Control-study/CNTENG
start linearization_simulator.html     # Windows
```

操作のポイント：

- **プリセット** で代表的なシナリオ (下死点・上死点・制御あり/なし) を一括設定．
- **物理パラメータ** (質量 $m$ , 長さ $l$ , 減衰 $k$ ) を編集．
- **平衡点** を下死点 ($\theta^\ast = 0$) または上死点 ($\theta^\ast = \pi$) から選択．
- **初期偏差** $\Delta\theta$ , $\Delta\dot{\theta}$ を設定して平衡点からのずれを指定．
- **状態フィードバック** を有効にすると，線形近似に基づく極配置で制御入力を生成．
- **▶ 計算・シミュレーション** ボタンでシミュレーションを実行し，アニメーションを再生．

---

## 非線形システムの線形近似

### 振り子の運動方程式

回転軸にトルク $\tau$ を与えることができる振り子の運動方程式は

```math
ml\ddot{\theta} = -mg\sin\theta - kl\dot{\theta} + \tau
```

状態 $x = [\theta, \dot{\theta}]^\top$ , 入力 $u = \tau$ とすると，

```math
f(x, u) = \begin{bmatrix} x_2 \\ -\frac{g}{l}\sin x_1 - \frac{k}{m}x_2 + \frac{1}{ml}u \end{bmatrix}
```

### 平衡点

$f(x^\ast, u^\ast) = 0$ を満たす点が平衡点です．振り子では

```math
x^\ast = \begin{bmatrix} n\pi \\ 0 \end{bmatrix}, \quad u^\ast = 0, \quad n = 0, 1, 2, \dots
```

- $n$ が偶数：**下死点** (自然に安定)
- $n$ が奇数：**上死点** (不安定)

### ヤコビ線形化

平衡点まわりで偏差変数を導入し，Taylor 展開の1次の項を取ると，線形近似システムが得られます．

```math
\dot{\bar{x}} = A\bar{x} + B\bar{u}, \quad \bar{x} = x - x^\ast, \quad \bar{u} = u - u^\ast
```

ここで $A$ , $B$ は $f$ のヤコビ行列です．

```math
A = \frac{\partial f}{\partial x}\bigg|_{x=x^\ast,\, u=u^\ast}, \quad B = \frac{\partial f}{\partial u}\bigg|_{x=x^\ast,\, u=u^\ast}
```

### 下死点と上死点の線形近似

**下死点** ($\theta^\ast = 0$)： $\sin\theta \approx \theta$ より

```math
A = \begin{bmatrix} 0 & 1 \\ -g/l & -k/m \end{bmatrix}, \quad B = \begin{bmatrix} 0 \\ 1/(ml) \end{bmatrix}
```

$A$ の固有値は負の実部をもち，平衡点は**安定**です．

**上死点** ($\theta^\ast = \pi$)： $\sin\theta \approx -(\theta - \pi)$ より

```math
A = \begin{bmatrix} 0 & 1 \\ g/l & -k/m \end{bmatrix}, \quad B = \begin{bmatrix} 0 \\ 1/(ml) \end{bmatrix}
```

$A$ の固有値に正の実部が含まれ，平衡点は**不安定**です．

---

## 線形近似の妥当性

線形近似は平衡点の**近傍でのみ有効**です．初期偏差が大きいと非線形項 ( $\sin\theta$ と $\theta$ の差) が無視できなくなり，線形近似の精度が低下します．

本シミュレータでは：
- **小偏差** ($\Delta\theta = 0.3$ rad) → 非線形と線形近似がほぼ一致
- **大偏差** ($\Delta\theta = 1.5$ rad) → 応答に大きな乖離が生じる

この違いをアニメーションと位相平面で直感的に確認できます．

---

## リアプノフの線形化法

非線形システム (N) $\dot{x} = f(x, u)$ と線形近似システム (L) の安定性の関係：

1. (L) が**漸近安定** ( $A$ の全固有値の実部が負) ならば，(N) は平衡点で**局所漸近安定**
2. (L) が**不安定** (正の実部をもつ固有値が存在) ならば，(N) は平衡点で**不安定**

上死点の線形近似は不安定極をもつため，非線形システムも上死点で不安定です．状態フィードバックで閉ループ極を安定領域に配置すれば，上死点まわりでの安定化 (倒立振子の制御) が可能になります．

---

## プリセット例の解説

### 下死点 (安定平衡点)

$\theta^\ast = 0$ , $\Delta\theta = 0.3$ rad．小偏差のため非線形と線形近似がほぼ一致し，減衰振動で平衡点に収束します．

### 下死点 (大偏差)

$\theta^\ast = 0$ , $\Delta\theta = 1.5$ rad．偏差が大きいため線形近似の精度が低下し，非線形応答と線形近似応答の間に乖離が生じます．

### 上死点 (不安定平衡点)

$\theta^\ast = \pi$ , $\Delta\theta = 0.1$ rad．制御なしでは小さな偏差でも発散し，線形近似も不安定を示します．

### 上死点＋状態FB安定化

$\theta^\ast = \pi$ , $\Delta\theta = 0.3$ rad，目標極 $P_1 = P_2 = -3$ ．状態フィードバックにより上死点 (倒立状態) を安定化します．線形近似に基づく制御器が非線形システムにも有効であることを確認できます．

---

## パラメータ一覧

### 入力パラメータ

| パラメータ | 説明 | 既定値 |
|---|---|---|
| $m$ | 質量 [kg] | $1$ |
| $l$ | 長さ [m] | $1$ |
| $k$ | 減衰係数 | $0.5$ |
| 平衡点 | 下死点 / 上死点 | 下死点 |
| $\Delta\theta$ | 初期角度偏差 [rad] | $0.3$ |
| $\Delta\dot{\theta}$ | 初期角速度偏差 [rad/s] | $0$ |
| 状態FB | 有効/無効 | 無効 |
| $P_1, P_2$ | 目標極 (実数) | $-3, -3$ |
| $T$ | シミュレーション時間 [s] | $10$ |

### 解析結果 (出力)

| 表示項目 | 説明 |
|---|---|
| 線形化行列 $A, B$ | 平衡点でのヤコビ行列 |
| $A$ の固有値 | 安定性を色分け表示 |
| フィードバックゲイン $K$ | 極配置で算出 (FB有効時) |
| 近似誤差 | 非線形と線形近似の最大角度差 |

### プロット

| プロット | 内容 |
|---|---|
| 振り子アニメーション | 非線形 (青) と線形近似 (橙) を同時に表示 |
| 位相平面 | 偏差角度 vs 角速度の軌跡 |
| 時間応答 | $\theta(t)$ の比較 |

### 数値計算手法

| 項目 | 手法 |
|---|---|
| 時間積分 | 4次 Runge-Kutta 法 (RK4) |
| ゲイン算出 | 特性多項式の係数比較 |
| アニメーション | requestAnimationFrame による描画 |

---

*この資料は付属の HTML シミュレータ (`linearization_simulator.html`) の理論的背景と操作方法を記述したものです．*
